export interface AddressReponse {
  street: string;
  neighborhood: string;
  city: string;
  state: string;
}

export type Address = {
  zipCode: string;
  street: string;
  number: string;
  district: string;
  city: string;
  state: string;
};
